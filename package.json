{
  "name": "integration-target-pr",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "npm run lint && node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "leftpad": "1.0.0"
  }
}
